#include <msp430.h>
#include "driver_aktorik.h"

void Driver_SetSteering ()
{

}

void Driver_SetThrottle ()
{
   /* if (throttle_counter <= 3)
        {
            TA1CCR1 = 2500;
        }
    else if (throttle_counter <= 131)
        {
            TA1CCR1 = 2500;
        }

    else if (throttle_counter <= 159)
        {
            TA1CCR1 = MAX_RPW + 500;
        }

    else if (throttle_counter <= 387)
        TA1CCR1 =
*/
}

